[package]
name = "win_file_reader_chrome_extension"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
winreg = "0.52"

[build-dependencies]
fs_extra = "1.2"
